# Optional for AI-powered rewriting. Heuristic analysis endpoints work without this.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# API behavior
TARGET_KEYWORD_MATCH=75
DEFAULT_REWRITE_MODE=transferable
